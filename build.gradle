plugins {
    id 'java'
}

group 'jp.naori.client'
version '0.1 BETA'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    // " testCompile group: 'junit', name: 'junit', version:'4.12 " does not work.
    implementation fileTree(dir: 'libs', include: '*.java')

}
